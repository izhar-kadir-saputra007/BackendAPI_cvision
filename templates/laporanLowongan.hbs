<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <style>
    body { 
      font-family: 'Arial', sans-serif;
      line-height: 1.6;
      color: #333;
    }
    .header { 
      text-align: center;
      margin-bottom: 25px;
      padding-bottom: 15px;
      border-bottom: 2px solid #3498db;
    }
    .title {
      font-size: 22px;
      font-weight: bold;
      color: #2c3e50;
      margin-bottom: 5px;
    }
    .subtitle {
      font-size: 16px;
      color: #7f8c8d;
    }
    .info-card {
      background: #f8f9fa;
      border-radius: 5px;
      padding: 15px;
      margin-bottom: 20px;
    }
    .info-row {
      display: flex;
      margin-bottom: 8px;
    }
    .info-label {
      font-weight: bold;
      width: 150px;
      color: #2c3e50;
    }
    .info-value {
      flex: 1;
    }
    .table-container {
      margin-top: 25px;
    }
    .pelamar-table {
      width: 100%;
      border-collapse: collapse;
      font-size: 14px;
    }
    .pelamar-table th {
      background-color: #3498db;
      color: white;
      padding: 10px;
      text-align: left;
    }
    .pelamar-table td {
      padding: 8px 10px;
      border: 1px solid #e0e0e0;
    }
    .pelamar-table tr:nth-child(even) {
      background-color: #f8f9fa;
    }
    .status-menunggu { color: #f39c12; }
    .status-diterima { color: #27ae60; }
    .status-ditolak { color: #e74c3c; }
    .footer {
      margin-top: 30px;
      text-align: right;
      font-size: 12px;
      color: #95a5a6;
      border-top: 1px solid #eee;
      padding-top: 10px;
    }
  </style>
</head>
<body>
  <div class="header">
    <div class="title">Laporan Pelamar Lowongan</div>
    <div class="subtitle">{{perusahaan}}</div>
  </div>

  <div class="info-card">
    <div class="info-row">
      <div class="info-label">Judul Lowongan:</div>
      <div class="info-value">{{judul}}</div>
    </div>
    <div class="info-row">
      <div class="info-label">Tanggal Buka:</div>
      <div class="info-value">{{tanggalBuka}}</div>
    </div>
    <div class="info-row">
      <div class="info-label">Tanggal Tutup:</div>
      <div class="info-value">{{tanggalTutup}}</div>
    </div>
    <div class="info-row">
      <div class="info-label">Jumlah Pelamar:</div>
      <div class="info-value">{{pelamar.length}} orang</div>
    </div>
  </div>

  <div class="table-container">
    <h3 style="margin-bottom: 10px;">Daftar Pelamar</h3>
    <table class="pelamar-table">
      <thead>
        <tr>
          <th width="5%">No</th>
          <th width="25%">Nama</th>
          <th width="30%">Email</th>
          <th width="20%">Status</th>
          <th width="20%">Tanggal Daftar</th>
        </tr>
      </thead>
      <tbody>
        {{#each pelamar}}
        <tr>
          <td>{{this.no}}</td>
          <td>{{this.nama}}</td>
          <td>{{this.email}}</td>
     <td class="status-{{this.status}}">
  {{#if this.status}}
    {{this.status}}
  {{else}}
    -
  {{/if}}
</td>
          <td>{{this.tanggalDaftar}}</td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>

  <div class="footer">
    Laporan dihasilkan pada: {{generatedAt}}
  </div>
</body>
</html>